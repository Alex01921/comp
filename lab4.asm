;Ascending order:
DATA SEGMENT
 STR1 DB 99H, 12H, 40H, 72H, 36H
DATA ENDS
CODE SEGMENT
 ASSUME DS:DATA, CS:CODE
START:
 MOV AX, DATA
 MOV DS, AX
 MOV CH, 04H
UP2:
 MOV CL, 04H
 LEA SI, STR1
UP1:
 MOV AL, [SI]
 MOV BL, [SI + 1]
 CMP AL, BL
 JC DOWN
 XCHG AL, BL
 MOV [SI], AL
 MOV [SI+1],BL
DOWN:
 INC SI
 DEC CL
 JNZ UP1
 DEC CH
 JNZ UP2
 MOV AH, 4CH
 INT 21H
 CODE ENDS
END START 

;DATA SEGMENT
 STR1 DB 99H, 12H, 40H, 72H, 36H
DATA ENDS
CODE SEGMENT
 ASSUME DS:DATA, CS:CODE
START:
 MOV AX, DATA
 MOV DS, AX
 MOV CH, 04H
UP2:
 MOV CL, 04H
 LEA SI, STR1
UP1:
 MOV AL, [SI]
 MOV BL, [SI + 1]
 CMP AL, BL
 JC SWAP
DOWN:
 INC SI
 DEC CL
 JNZ UP1
 DEC CH
 JNZ UP2
 MOV AH, 4CH
 INT 21H
SWAP:
 XCHG AL, BL
 MOV [SI], AL
 MOV [SI + 1], BL
 JMP DOWN
 CODE ENDS
END START 
